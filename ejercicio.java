public class Ejer{
  /*Dada una matriz que representa un almacén donde cada celda contiene un objeto Producto con 
  atributos nombre, precio y cantidad, escribe un algoritmo que encuentre la ubicación 
  (fila, columna) de un producto en una lisa de 30 productos buscar específico por su nombre. 
Solución: Recorre la matriz buscando el Producto cuyo nombre coincida con el buscado. Devuelve la
 posición de la celda. 
   */
  class Producto {
    String nombre;
    double precio;
    int cantidad;
public int [][] LlenarMatrizAlamcen(int [][]A){
        for ( int i = 0; i < m.length; i++){
new Producto();
            for (int j = 0; j < m.length; j++){
                m[i][j] = ;
            }
            
        }
        return A;
    }
}